============
Installation
============

At the command line::

    $ pip install fsa_test_one

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv fsa_test_one
    $ pip install fsa_test_one
